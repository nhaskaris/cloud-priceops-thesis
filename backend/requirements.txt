# Core framework
Django
djangorestframework
django-cors-headers
python-dotenv
whitenoise

# Database
psycopg[binary,pool]

# Networking / API
requests

# Background tasks
celery
redis
flower

# Feast + DuckDB for feature store (SDK-first)
feast[postgres, redis]
duckdb>=0.6.1

# Utilities
tenacity==8.2.2

# WSGI server
gunicorn